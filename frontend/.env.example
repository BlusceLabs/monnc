# Public frontend env (exposed to the browser via the PUBLIC_ prefix).
# Points the Astro app at the Python backend. No TMDB key needed here.
PUBLIC_API_BASE=http://localhost:8028
PUBLIC_TMDB_LANGUAGE=en-US
PUBLIC_TMDB_REGION=US
